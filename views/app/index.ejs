<!DOCTYPE html>
<html lang="en">
<head>
    <!-- inclusion d'un fichier en lui passant des donnees-->
    <%- include('../partial/head', {title: "Welcome to my index page !"}); %>
</head>
<body>
    <!-- inclusion d'un fichier-->
    <%- include('../partial/header'); %>
    <h1>Page d'accueil</h1>
    <h2>Date et heure : <%= `${date.getDate()}/${date.getMonth()}/${date.getFullYear()} ${date.getHours()}:${date.getMinutes()}:${date.getSeconds()}` %></h2>
    <h2>Nom : <%= name %></h2>

    <h3>Recete</h3>
    <ul>
        <% for( let index = 0; index < list.length; index++ ) { %>
            <li><%= list[index] %> </li>
        <% } %>
    </ul>
    <%- include('../partial/footer') %> 
</body>
</html>